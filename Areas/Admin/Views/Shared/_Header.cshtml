@using System.Security.Claims

<div class="top">
    <button id="menu-btn">
        <span class="material-icons-sharp">menu</span>
    </button>
    <div class="theme-toggler">
        <span class="material-icons-sharp active">light_mode</span>
        <span class="material-icons-sharp">dark_mode</span>
    </div>
    <div class="profile">
        <div class="info">
            <p>Xin Chào: <b>@User.Identity.Name</b></p>
            <small class="text-muted">Vai trò: @User.FindFirst(ClaimTypes.Role)?.Value</small>
        </div>
        <div class="profile-photo">
            @if (User.Identity.IsAuthenticated)
            {
               
                <img src="@Url.Content($"/{User.FindFirst("UserImage")?.Value}")" alt="avatar">
            }
            else
            {
                <img src="~/media/icons/logo_dragon.jpg" alt="default-avatar">
            }
        </div>
    </div>
</div>
